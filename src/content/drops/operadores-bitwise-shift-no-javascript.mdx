---
templateKey: drop-post
title: Operadores Bitwise Shift no JavaScript
slug: operadores-bitwise-shift-no-javascript
featured:
  - https://res.cloudinary.com/beto-muniz/image/upload/f_auto/v1600178580/Titulo_Image_Site_1_twky3b.jpg
description: Neste drop abordo de forma objetiva o que são e como se comportam
  Operadores Bitwise Shift.
date: 2020-09-17T13:04:25.598Z
keywords:
  - bitwise
  - javascript
  - operadores
  - operators
  - right
  - left
  - zero
audiencecall: E aí gente, beleza?
informaldesc: 💥 No drop de hoje compilei e expliquei o que são e como se
  comportam *Operadores Bitwise Shift* no JavaScript. Apesar de ser um assunto
  bem denso, teórico e sem grandes casos de uso, acredito que ficou beeem legal.
category: tech
lang: pt
external: false
---
import { Drop } from '../../componenets/mdx'

<Drop>

Conceitos teóricos a parte, **operadores bitwise**, como o nome já faz referência, são operações efetuadas bit a bit em dados númericos e sempre baseadas em 32bits.

Esse tipo de operador é útil em algoritmos de _checksum_ que validam a integridade dos dados, máquinas de estados, criptografia, cálculos com cores, etc. 

Falarei apenas dos **Operadores Bitwise Shift** `<<`, `>>` e `>>>`, que são utilizados para deslocar bits da esquerda pra direita.

> Dica de pesquisa: Existem outros operadores bitwise.

E agora sim, considerando que `170` em bits é `00000000000000000000000010101010`, vejamos as seguintes definições:

</Drop>

<Drop>

`<<` ou _Left Shift_ recebe `x` e `y` para `x << y`. Enquanto `x` é o número inteiro a ser processado, `y` é o número de bits a ser deslocado a partir da esquerda.

```text
170 << 3

(000)00000000000000000000010101010(***)
(***)00000000000000000000010101010(000)

R: 00000000000000000000010101010000
```

<small style={{ textAlign: 'center', marginTop: '-25px', marginBottom: '20px'}}>
Exemplo retirado de <wbr /><a href="https://blog.logrocket.com/interesting-use-cases-for-javascript-bitwise-operators">blog.logrocket.com</a>
</small>

</Drop>


<Drop>

`>>` ou _Right Shift_ recebe `x` e `y` para `x >> y`. Enquanto `x` é o número inteiro a ser processado, `y` é o número de bits a ser deslocado a partir da direita.

```text
170 >> 3

(***)00000000000000000000000010101(010)
(000)00000000000000000000000010101(***)

R: 00000000000000000000000000010101
```

<small style={{ textAlign: 'center', marginTop: '-25px', marginBottom: '20px'}}>
Exemplo retirado de <wbr /><a href="https://blog.logrocket.com/interesting-use-cases-for-javascript-bitwise-operators">blog.logrocket.com</a>
</small>

</Drop>


<Drop>

`>>>`ou _Zero-Fill Right Shift_ é semelhante a `>>`, com a diferença que sempre irá popular `0` na definição esquerda do bit, `>>` pode ser `0` ou `1`. 

`>>>` recebe `x` e `y` para `x >>> y`. Enquanto `x` é o número inteiro a ser processado, `y` é o número de bits a ser deslocado a partir da direita.

```text
170 >>> 3

(***)00000000000000000000000010101(010)
(000)00000000000000000000000010101(***)

R: 00000000000000000000000000010101
```

<small style={{ textAlign: 'center', marginTop: '-25px', marginBottom: '20px'}}>
Exemplo retirado de <wbr /><a href="https://blog.logrocket.com/interesting-use-cases-for-javascript-bitwise-operators">blog.logrocket.com</a>
</small>

</Drop>